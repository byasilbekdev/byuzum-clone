import { Card } from "@/components/ui/card";

const page = () => {
  return (
    <Card className="glass glass-hover p-6 flex items-center justify-center flex-col gap-4 w-100 h-100">
      hello
    </Card>
  );
};

export default page;
